using System.Windows;

namespace AutoF11;

/// <summary>
/// Main window (hidden, app runs in tray).
/// </summary>
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
